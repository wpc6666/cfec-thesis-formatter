# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范：`主版本.次版本.修订号`。

---

## [1.1.0] - 2026-09-02

### 新增

- **manifest.json**：新增 Skill 元信息配置文件，支持豆包 Agent 覆盖安装识别，包含版本号、更新模式、资源清单等字段。
- **Step 0 前置检查**：新增输入完整性检查、环境检查、格式规范加载三个子步骤，确保处理前所有依赖就绪。
- **错误处理与回退机制**：新增常见错误场景对照表及降级原则，保证至少一个版本完整输出。
- **增强版自检清单**：DOCX 和 LaTeX 各新增 8+ 项检查点，覆盖易错格式细节。
- **交付说明规范**：明确交付时必须包含的 5 项内容（文件列表、调整项、自检结果、待确认事项、编译说明）。
- **CHANGELOG.md**：新增版本更新日志文件。

### 优化

- SKILL.md 头部元信息新增 `version: 1.1.0` 字段。
- 工作流步骤编号统一，从 Step 0 开始，逻辑更清晰。
- 核心原则新增"可追溯"原则，强调每步格式调整有明确依据。
- 允许与禁止列表更新，新增"输出格式自检报告"允许项和"未告知省略输出版本"禁止项。

### 修复

- 无（本版本为功能增强版，未修复已知 bug）。

---

## [1.0.0] - 初始版本

### 功能

- 重庆财经学院本科毕业论文格式规范完整实现。
- 双版本输出：DOCX（Word）+ LaTeX（XeLaTeX）。
- 四级标题体系：一、 →（一）→ 1. →（1）。
- 页码分节：封面无码、前置罗马数字、正文阿拉伯数字。
- 图表编号：图 1-1 / 表 1-1，按章节编号。
- 公式编号：（1.1）右对齐，按章节编号。
- 参考文献：悬挂缩进 2 字符，正文引用上标。
- 目录：只显示到二级标题，点号前导符。
- LaTeX 模板：基于 ctexart + XeLaTeX，内置自定义标题命令。
- 格式规范速查表：references/format-spec.md。
- LaTeX 使用指南：references/latex-guide.md。

---

## 版本说明

- **主版本号**：不兼容的 API 或格式规范重大变更时递增。
- **次版本号**：向下兼容的功能性新增时递增。
- **修订号**：向下兼容的问题修正时递增。

### 覆盖安装说明

本 Skill 支持覆盖安装（`update_mode: overwrite`）。安装 v1.1.0 时：

1. 豆包 Agent 读取 manifest.json，识别 `id: cfec-thesis-formatter`。
2. 检测到已安装同 ID 旧版本，自动覆盖 SKILL.md 及配套资源。
3. 版本号更新为 1.1.0，旧版本被替换。
4. 重启 Agent 服务后生效。

> 注意：覆盖安装不会删除用户自定义的论文文件，仅更新 Skill 本身。
